# tool_name: read_file
# created_at: 2026-09-13T10:40:21.358202
# ---
# Sanity — read-only, build-time (SSG). projectId is public/safe.
PUBLIC_SANITY_PROJECT_ID=p3d22f8w
PUBLIC_SANITY_DATASET=production
SANITY_API_VERSION=2024-10-01

# Soft-launch search-engine switch. Keep false while the client is learning the
# Studio and content is still being completed. For the public indexing launch,
# change this to true, remove the soft-launch X-Robots-Tag rule in
# public/_headers, then rebuild and redeploy.
PUBLIC_SITE_INDEXING_ENABLED=false

# SANITY_READ_TOKEN — leave UNSET while the dataset is public (current setup).
# Only needed if you later switch the dataset to private. When set:
#   - use a READ-only (Viewer) token — NEVER the Studio write token
#   - no PUBLIC_ prefix: it is server-only and must never reach the client bundle
#   - the client then bypasses the CDN automatically (useCdn:false)
# SANITY_READ_TOKEN=

# Quote / contact form submission. Powers the PDP "Request a quote" pop-up AND
# the /contact inquiry form and RFQ cart → Astro Actions → verify Turnstile →
# persist the inquiry to D1 → send via Resend. Uploaded files are attached to
# the sales email; an optional R2 binding can be added later for file retention.
# In production set these as encrypted
# Worker secrets; locally put them here or in .dev.vars. All are
# OPTIONAL — the site builds & the modal opens without them; submission just
# shows "email us directly" until they're set.
#
# Resend (https://resend.com) — server-only secrets:
#   RESEND_API_KEY  — API key (starts with "re_")
#   QUOTE_FROM_EMAIL — verified sender, e.g. "Coral Pilates <quotes@coralpilates.com>"
#   QUOTE_TO_EMAIL   — inbox that receives leads (e.g. Chris's address)
# RESEND_API_KEY=
# QUOTE_FROM_EMAIL=
# QUOTE_TO_EMAIL=
#
# Cloudflare Turnstile (https://dash.cloudflare.com → Turnstile):
#   TURNSTILE_SECRET_KEY       — server-only secret (verifies the token)
#   PUBLIC_TURNSTILE_SITE_KEY  — public; renders the widget in the browser
# Local test keys that always pass: site 1x00000000000000000000AA /
#   secret 1x0000000000000000000000000000000AA
# TURNSTILE_SECRET_KEY=
# PUBLIC_TURNSTILE_SITE_KEY=

# Cloudflare bindings are declared in wrangler.jsonc:
#   INQUIRIES_DB — private D1 inquiry records and attachment metadata
# Optional later phase:
#   INQUIRY_FILES — private R2 floor plans / RFQ attachments
# Apply the local schema with `npm run db:migrate:local`.
# After authenticating Wrangler, provision/deploy the resources and apply the
# production schema with `npm run db:migrate:remote`.
