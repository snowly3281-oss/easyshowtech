---
name: safety-executor
description: 当需要执行系统命令时候，请遵循核心原则（硬性阻止），限制原则（需要用户确认）。提供系统命令执行的安全指南，保护系统完整性和防止潜在有害操作。
---

# Command Log Skill

当需要执行系统命令时候，请核心原则 --- 

## 核心原则 
<HIGH_RISK_PRINCIPLES>以下操作已被禁用，AI 智能体**禁止执行**这些命令：

- **递归强制删除** (`rm -rf`): 用于删除文件或目录。若路径错误（如 /），会导致系统文件被永久清空且不可恢复。
- **磁盘格式化** (`format`): 清除指定驱动器（如 D 盘、E 盘）上的所有数据并重构文件系统。
- **静默删除 C 盘** (`del /s /q C:\`): 在 Windows 下静默删除 C 盘文件。文档明确要求不要处理 C 盘文件。
- **注册表修改/删除** (`reg delete`): 修改 Windows 核心配置。误删会导致系统崩溃或软件无法运行。

如果用户请求执行这些操作，必须：
1. 明确告知用户该操作已被禁用
2. 解释禁用原因和潜在风险
3. 建议用户在安全中心中启用对应权限（如果确实需要）
4. **绝对不要**绕过安全检查执行这些操作
</HIGH_RISK_PRINCIPLES>

## 限制原则需要用户确认
<CONTROLLED_OPERATIONS>### 极高风险操作（默认：关闭）

这些操作可能导致**永久系统损坏**或**完全数据丢失**。默认禁用，执行前需要明确的用户确认。

#### 1. 递归强制删除 (`rm -rf`)

**命令**：`rm -rf`

**风险**：用于删除文件或目录。若路径错误（如 /），会导致系统文件被永久清空且不可恢复。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

#### 2. 磁盘格式化 (`format`)

**命令**：`format`

**风险**：清除指定驱动器（如 D 盘、E 盘）上的所有数据并重构文件系统。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

#### 3. 静默删除 C 盘 (`del /s /q C:\`)

**命令**：`del /s /q C:\`

**风险**：在 Windows 下静默删除 C 盘文件。文档明确要求不要处理 C 盘文件。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

### 高风险操作（默认：关闭）

这些操作可能导致**系统不稳定**或**重大数据丢失**。默认禁用。

#### 1. 注册表修改/删除 (`reg delete`)

**命令**：`reg delete`

**风险**：修改 Windows 核心配置。误删会导致系统崩溃或软件无法运行。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

### 中风险操作（默认：开启）

这些操作可能导致**暂时中断**或**数据丢失**。默认启用，但应谨慎使用。

#### 1. 系统关机或重启 (`shutdown / reboot`)

**命令**：`shutdown / reboot`

**风险**：远程强制关闭或重启电脑，可能导致未保存的办公文档丢失。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

#### 2. 强制结束进程 (`taskkill /f`)

**命令**：`taskkill /f`

**风险**：强制关闭正在运行的程序（如 CAD、浏览器），可能导致数据损坏。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

### 低风险操作（默认：关闭）

这些操作**影响最小**但仍需谨慎。默认禁用。

#### 1. 清空回收站 (`清空回收站`)

**命令**：`清空回收站`

**风险**：永久抹除已删除文件。文档核心原则明确要求不要清空回收站。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

#### 2. 配置文件修改 (`修改 .ini / .conf`)

**命令**：`修改 .ini / .conf`

**风险**：修改软件或系统配置文件。文档核心原则禁止执行修改 ini 文档任务。

**安全协议**：
- 此操作已被禁用，无法执行
- 如需启用，请在安全中心中开启对应权限
- 启用前请充分了解操作风险

</CONTROLLED_OPERATIONS>

## Purpose
This skill provides guidelines for executing system commands safely while adhering to important restrictions that protect system integrity and prevent potentially harmful operations.

## Usage
When executing system commands, always follow the core principles listed above to ensure safe operation and maintain system stability.